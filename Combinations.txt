# Combinations
# Platform: LeetCode
# Language: 1class Solution:
2    def generate_subsequence(self,index,nums,subset,result,k):
3        if len(subset)==k:
4            result.append(subset[:])
5            return 
6        if index>=len(nums):
7            return 
8        subset.append(nums[index])
9        self.generate_subsequence(index+1,nums,subset,result,k)
10        subset.pop()
11        self.generate_subsequence(index+1,nums,subset,result,k)
12    def combine(self, n: int, k: int) -> List[List[int]]:
13        result=[]
14        nums=[]
15        for i in range(1,n+1):
16            nums.append(i)
17        self.generate_subsequence(0,nums,[],result,k)
18        return result

class Solution:
    def generate_subsequence(self,index,nums,subset,result,k):
        if len(subset)==k:
            result.append(subset[:])
            return 
        if index>=len(nums):
            return 
        subset.append(nums[index])
        self.generate_subsequence(index+1,